@model IEnumerable<CompanyEmployees.Client.Models.ClienteViewModel>
@{
    ViewData["Title"] = "Clientes";
}

<h1>Cliente</h1>

<p>
    <a asp-controller="home" asp-action="create">Create Cliente</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Codigo</th>
            <th>Nombres</th>
            <th>
                Apellidos

            </th>
            <th>
                Documento
            </th>
            <th>
                Nùmero
            </th>
            <th>
                Direcciòn
            </th>
            <th>
                Telefonò
            </th>
            <th>
                Email
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.codigoCliente)</td>
                <td>@Html.DisplayFor(modelItem => item.nombres)</td>
                <td>@Html.DisplayFor(modelItem => item.apellidos)</td>
                <td>@Html.DisplayFor(modelItem => item.documento)</td>
                <td>@Html.DisplayFor(modelItem => item.numero)</td>
                <td>@Html.DisplayFor(modelItem => item.direccion)</td>
                <td>@Html.DisplayFor(modelItem => item.telefono)</td>
                <td>@Html.DisplayFor(modelItem => item.email)</td>

                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.codigoCliente })
                    @Html.ActionLink("Details", "Edit", new {/*------*/ })
                    <a asp-controller="home" asp-action="delete" asp-route-id="@item.codigoCliente">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
